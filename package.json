{
  "name": "simcompanies-helper",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "pack": "node scripts/pack-extension.mjs",
    "release:local": "npm run build && npm run pack"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "archiver": "^7.0.1"
  }
}
