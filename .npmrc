# Supply chain hardening

# Prevent install-time scripts from running (blocks malware like CVE-2025-54313)
ignore-scripts=true

# Require lockfile to be present and up-to-date (prevents phantom dependency drift)
package-lock=true

# Warn on known vulnerabilities during install
audit=true

